export default function (org) {
    return (
        <div className="header">
            <h1 className="title">{ org.title}</h1>
            <br/>
            <p className="subtitle">{org.subtitle}</p>
            
        </div>
 
    )   
                       
}